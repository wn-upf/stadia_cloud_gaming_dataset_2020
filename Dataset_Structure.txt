For folders {V-VIII.A}:

Arrival time (epoch) || Inter-arrival time (s) || UDP frame length (bytes)

For Folder {VIII.B}:

Video Frame Height || Frames Per Second || Round Trip Time (s) || Packets lost

For Folder{IX}:

Round Trip Time (s) || Jitter Buffer Delay (ms)
